# Section 5.4 — B3 space–time fractional case + PINO residual-weight sweep

`b3_pino.py` compares FrFNO and FNO (time-fractional treatment) against PINO with
a physics-residual weight lambda in {0, 0.5}, at three evaluation tiers.

Outputs: `b3_result_lam0.txt` (lambda=0) and `b3_result_lam0.5.txt` (lambda=0.5).
