# Figures

`make_modal_decay_fig.py` is a pure analytic schematic (numpy/scipy/matplotlib,
no training, no network): it plots the Mittag-Leffler modal amplitude versus T
for several orders, and versus mode at fixed order. It writes
`modal_decay_vs_T.pdf/.png` next to itself; copy them to `paper/figs/`.
